# Fex Compiler

A compiler written in C++ for my own programming language: Fex.

## Build from source:

Using CMake:

```
git clone https://github.com/paul-csc/Fex-Compiler.git Lexc
cd Lexc
cmake -S . -B build
cmake --build build
```
